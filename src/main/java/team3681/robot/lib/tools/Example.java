package team3681.robot.lib.tools;

public class Example {
    
    /**
     * Variable amount of constructors
     * 
     * Instead of 'new SparkWrapper(blah blah blah blah)' and I dont need that many. I want it to have default values
     * instead I create an object 
     * UniversalMotor deezer = new SparkBuilder(port, name) //required information
     * .withIdleMotorType("blah")
     * .withConfigWhatever4("");
     * 
     * instead of needing (deez, deez, deez, deez)
     * 
     * then I need to implement it with the factory method to allow for better code.
     */
}
